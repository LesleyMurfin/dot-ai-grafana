Playwright now runs a by-design e2e suite over the real `/api/plugins/devopstoolkit-dotai-app/resources/*` path as Viewer, Editor and Admin against a stubbed dot-ai upstream, so a role-gate regression on the tool routes fails CI; the deny cases assert the denial happened with no upstream dial by reading the stub's hit counters rather than inferring it from the response body.
