The 0-hop navigation classifier no longer accepts "alerts" or "dashboards": the skip path can only answer with Explore/Drilldown links, which cover logs, metrics and traces, so those two nouns take the normal dot-ai path instead of being answered with unrelated links. Duplicate trace ids in Tempo evidence now produce a single link, and trace labels fall back to the full id when two ids share an 8-character prefix.
