Query Asks that produce Grafana stack evidence now render Explore and Drilldown links (logs, metrics, traces) in the Map panel. The Map panel (evidence hints plus these links) now renders above "Current", which is collapsed by default behind a toggle, so evidence-heavy answers do not push the response off screen.
