Corrected the **Send Grafana evidence** row in the Configure table: the toggle scopes Query only — a Query Ask that reads the stack replaces Current with fresh Grafana datasource facts, and Remediate never reads a datasource whether the toggle is on or off.
