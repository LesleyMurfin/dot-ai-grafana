Follow-up Asks now carry the conversation. `ToolThread.history` was maintained and shown on screen but never packed, so every follow-up reached dot-ai with no prior context and referents such as "the first one" could not resolve; a condensed `Prior:` block of at most 240 characters, built from the two most recent turns, now travels inside the unchanged 1000-character intent budget. That block is prior-turn egress — the operator's own question text and prose from dot-ai's answers, which quote log, metric and alert lines verbatim — so the on-page notice and the "Send Grafana evidence" description now state what actually leaves the browser, including that turning evidence off stops the datasource read but not `Prior:`, `Map:` or the rewritten `Current:`. When the budget binds, `Map:` goes first and `Prior:` is shrunk before evidence is peeled; if `Prior:` is dropped entirely the evidence reduction restarts from the untouched Current, so log, metric and alert lines are never spent on a block that does not ship. The two on-page strings and the config description were corrected again: a Query Ask that reads fresh Grafana datasource facts replaces Current with them rather than sending both, Remediate never reads a datasource regardless of the toggle, and the question side of a `Prior:` pair can also carry follow-up instructions this page adds automatically (across-cluster search, conflict/hedge corrections), not just the operator's typed text. Each condensed turn is also held to the budget its newer siblings left: `formatPriorPair`'s 12-char answer floor previously let an older turn return up to ~19 chars more than its share, and because the lines are joined chronologically the resulting overflow was trimmed off the tail — the newest turn, the one the referent is needed from. The per-line cap and the reserved join newline mean a shortfall is now paid by the oldest turn, and `Prior:` still reaches its full 240 characters.
