Show-me navigation Asks now fail loudly instead of claiming Grafana evidence they do not hold: the 0-hop path reports an explicit failure when "Send Grafana evidence" is off and when the datasource read returned nothing with no drilldown links. The `hops` request field keeps its original meaning (the planned hop cap), distinct from `hop` (the current hop).
