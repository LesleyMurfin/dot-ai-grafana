Query and Remediate now require Grafana org **Editor** or **Admin**. Viewer and org role `None` are denied with HTTP 403 before any call to the dot-ai server, so operators must grant Editor to anyone who needs Ask. Configuration and Test connection remain Admin-only.
