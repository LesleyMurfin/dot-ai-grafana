PRD #1 and `CLAUDE.md` now record the text-only *rendering* decision as superseded: the agent's answer is rendered as sanitized GFM markdown, so the standing "no rich visualizations (Mermaid, cards, tables) — just plain text responses" wording no longer describes the design. The *request* half is unchanged and now says so explicitly — the plugin sends the plain intent and never prefixes `[visualization]`, so it only renders markdown dot-ai already emits. Mermaid, cards and client-side charts stay out of scope. Navigation milestones read as decided rather than shipped, since that code is still in review.
