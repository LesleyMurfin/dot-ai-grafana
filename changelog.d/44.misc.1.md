The by-design e2e suite gains its Phase 2 privacy and consent coverage: the config page never renders the provisioned bearer token into the DOM, the Debug Log switch is off on a fresh load, no `execute`/`operate` resource route exists on the real HTTP path, the on-screen History panel shows the full verbatim question even when the wire's condensed `Prior:` block would have to truncate it, and that `Prior:` block itself is now measured on the wire and pinned at `MAX_PRIOR_CHARS` (240) independent of the overall 1000-char intent budget already covered — so a regression in any of these now fails CI instead of only Go unit tests or nothing at all.
