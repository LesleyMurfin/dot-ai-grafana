Alertmanager evidence now actually loads. `getDataSourceByType` listed candidates with `getDataSourceSrv().getList({ type })`, and Grafana's `getList` drops every datasource whose plugin meta declares none of `metrics`/`annotations`/`tracing`/`logs`/`alerting` unless `all: true` is passed — which is exactly Grafana's built-in Alertmanager, whose `plugin.json` declares only `"metrics": false`. The alert lines the preamble asks dot-ai to quote were therefore never read on any stack, and `Current:` silently shipped without them; the list call now passes `all: true`, and because selection still filters by `type` and prefers the Grafana default, which datasource is picked for Loki, Prometheus and Tempo is unchanged.
