Alertmanager evidence now actually loads. `getDataSourceByType` listed candidates with `getDataSourceSrv().getList({ type })`, and Grafana's `getList` drops every datasource whose plugin meta declares none of `metrics`/`annotations`/`tracing`/`logs`/`alerting` unless `all: true` is passed — which is exactly Grafana's built-in Alertmanager, whose `plugin.json` declares only `"metrics": false`. The alert lines the preamble asks dot-ai to quote were therefore never read on any stack, and `Current:` silently shipped without them; the list call now passes `all: true`, and because selection still filters by `type` and prefers the Grafana default, which datasource is picked for Loki, Prometheus and Tempo is unchanged. Bringing `upstream/main` in (`8f553bb`) conflicted on one file only, `tests/harness/dot_ai_stub.py`, where both sides had appended their own paragraph to the module docstring at the same offset — this branch's INTENT RECORDER, served from `GET /intents`, and upstream's `TRIGGER_UPSTREAM_TRANSPORT_ERROR` transport-failure trigger from #56 — so both are kept, in code order, because both still describe harness behaviour with a live e2e caller and dropping either would have left it undocumented; the branch merges cleanly on top of that history.
