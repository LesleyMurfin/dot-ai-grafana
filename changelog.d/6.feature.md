Grafana evidence now surfaces as Explore and Drilldown links in a "Map" panel above the response, and "Current" moved below it into a collapsed toggle so evidence-heavy answers no longer push the answer off screen. Pure navigation Asks ("show me the logs", "open traces", "display metrics", "show me the alerts", "show me the dashboards") resolve without a dot-ai hop when Map already has a matching link: alerts open Grafana's `/alerting/list`, dashboards open `/d/<uid>` from firing-alert annotations. "show me the dashboards" still POSTs when no firing alert carried a dashboard UID, so it does not skip the engine into unrelated logs or metrics links. Diagnosis language (why, error, crash, analyze and related tokens) always takes the normal dot-ai hop. The same three explicit failures remain: "Send Grafana evidence" off, Grafana stack read failed, and a successful read that produced neither evidence nor links.
