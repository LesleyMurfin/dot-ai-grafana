Grafana evidence now surfaces as Explore and Drilldown links in a "Map" panel above the response, and "Current" moved below it into a collapsed toggle so evidence-heavy answers no longer push the answer off screen. Pure navigation Asks ("show me the logs", "open traces", "display metrics") resolve without a dot-ai hop when there is evidence to point at, and return an explicit error rather than an empty answer when there is not: when "Send Grafana evidence" is off, when the Grafana stack read failed, and when the read succeeded but produced neither evidence nor links. Diagnosis language (why, error, crash, analyze and related tokens) always takes the normal dot-ai hop.
