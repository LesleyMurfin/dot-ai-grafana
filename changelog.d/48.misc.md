The unit tests no longer depend on `window.location` being replaceable, on `@grafana/ui` rendering its `Switch` as `role="checkbox"`, or on jsdom providing `IntersectionObserver` — which `@grafana/ui` 11.6 needs because its `Combobox` renders a `ScrollContainer` — so they keep passing under the jsdom 26 and `@grafana/ui` 11.6 bumps.
