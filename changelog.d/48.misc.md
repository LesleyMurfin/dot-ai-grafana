The `AppConfig` unit tests no longer depend on `window.location` being replaceable or on `@grafana/ui` rendering its `Switch` as `role="checkbox"`, so they keep passing under the jsdom 26 and `@grafana/ui` 11.6 bumps.
