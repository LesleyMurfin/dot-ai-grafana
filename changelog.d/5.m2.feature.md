Remediate analysis results now expose an explicit Propose GitOps PR action that previews title, body, and a top-level values diff. The action stays disabled with a reason when `GET /gitops-status` reports `ready=false`. `POST /gitops-propose` returns a dry preview only — it does not call GitHub or apply to the cluster. Creating the PR is M3.
