Long Query and Remediate asks now show which hop and stage is running (hop N of 3, plus first / across clusters / conflict / hedge / refine, or a single analysis hop on Remediate) instead of a static “Waiting for dot-ai…” spinner. The copy comes from the orchestrator’s real hop loop — there is no percent bar or invented elapsed timer.
