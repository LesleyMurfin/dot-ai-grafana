In-flight Asks no longer show a static "Waiting for dot-ai…" spinner. Query names the real stage — Grafana evidence read, then hop 1–3 (initial / across-clusters / conflict / hedge / refine) — and Remediate names analysis; elapsed time sits next to the copy so a multi-minute call is visibly still working. Follow-up context (Prior packing, Clear thread) was already on `main`.
