`scripts/ci-local.sh` reproduces the CI gate suite on a developer machine from one declarative gate registry, distinguishing a skipped gate (exit 2 — no Docker daemon, no signing token) from a genuine pass, and its `drift` gate (`scripts/ci-drift-check.sh`) fails in either direction if `.github/workflows/ci.yml` and that registry stop matching.
