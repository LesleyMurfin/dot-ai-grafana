The text-only decision is retired for **rendering** only, and only once PR #51 lands. `CLAUDE.md`, `README.md` and PRD #1 now record that the response area is plain text on `main` and renders the model's own GFM markdown as sanitized HTML — code blocks unhighlighted — from #51 onward. The **request**-side ban is unchanged and holds today: the plugin never prefixes `[visualization]` to the intent, so dot-ai is never asked for rich visualizations.
